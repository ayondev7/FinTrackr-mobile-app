const BASE = '/users';

export const userRoutes = {
  profile: `${BASE}/profile`,
  updateProfile: `${BASE}/profile`,
  updateBalance: `${BASE}/balance`,
  updateNotificationSettings: `${BASE}/notification-settings`,
  deleteAccount: `${BASE}/account`,
  clearData: `${BASE}/data`,
  exportData: `${BASE}/export`,
};
